{%extends "CdlrcodeBundle::layout.html.twig" %}
{% block content %}
        <div align="center">

    <h1>CoursView list</h1>

    <table class="records_list" border="1">
        <thead>
            <tr>
                <th>Note</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('coursview_show', { 'id': entity.id }) }}">{{ entity.note }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('coursview_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('coursview_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('coursview_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
                    </div>

    {% endblock %}
