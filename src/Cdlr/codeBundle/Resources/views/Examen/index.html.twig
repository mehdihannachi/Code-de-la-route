{%extends "CdlrcodeBundle::layout.html.twig" %}
{% block content %}
    <div align="center">
    <h1>Examen list</h1>

    <table class="records_list" border="1">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Type</th>
                <th>Examenid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('examen_show', { 'id': entity.examenId }) }}">{{ entity.nom }}</a></td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.examenId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('examen_show', { 'id': entity.examenId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('examen_edit', { 'id': entity.examenId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('examen_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
                </div>
    {% endblock %}
